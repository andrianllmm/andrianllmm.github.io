# CV

This folder contains the source code for my CV.

To render:

```bash
cd cv # IMPORTANT: change directory to cv folder
source venv/bin/activate # activate virtual environment
npm run cv # watch for changes and render
```
