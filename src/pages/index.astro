---
import { PINNED_PROJECTS, PINNED_SKILLS, SITE, HOME, SOCIALS } from "@consts";
import { getCollection } from "astro:content";
import Layout from "@layouts/Layout.astro";
import Container from "@components/Container.astro";
import Link from "@components/Link.astro";
import ProjectCard from "@components/ProjectCard.astro";
import PostCard from "@components/PostCard.astro";
import AchievementCard from "@components/AchievementCard.astro";
import EducationCard from "@components/EducationCard.astro";
import SkillBadge from "@components/SkillBadge.astro";
import TagBadge from "@components/TagBadge.astro";
import Badge from "@components/Badge.astro";
import Avatar from "@components/Avatar.astro";
import Contact from "@components/Contact.astro";
import StructuredData from "@components/StructuredData.astro";
import { tags as allTags } from "@data/tags";
import { education } from "@data/education";

const skills = PINNED_SKILLS;
const tags = Object.keys(allTags).slice(0, SITE.NUM_TAGS_ON_HOMEPAGE);

const allProjects = (await getCollection("projects")).filter(
  (p) => !p.data.draft,
);
const projects = PINNED_PROJECTS.map((id) =>
  allProjects.find((p) => p.id === id),
)
  .filter((p): p is (typeof allProjects)[number] => p !== undefined)
  .slice(0, SITE.NUM_PROJECTS_ON_HOMEPAGE);

const allPosts = (await getCollection("posts")).filter((p) => !p.data.draft);
const posts = allPosts
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, SITE.NUM_POSTS_ON_HOMEPAGE);

const allAchievements = (await getCollection("achievements")).filter(
  (a) => !a.data.draft,
);
const achievements = [...allAchievements]
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, SITE.NUM_ACHIEVEMENTS_ON_HOMEPAGE || 3);

const stillLearning = education
  .sort((a, b) => {
    const aEnd = a.endDate ? new Date(a.endDate).getTime() : Infinity;
    const bEnd = b.endDate ? new Date(b.endDate).getTime() : Infinity;
    if (bEnd !== aEnd) return bEnd - aEnd;
    const aStart = a.startDate ? new Date(a.startDate).getTime() : 0;
    const bStart = b.startDate ? new Date(b.startDate).getTime() : 0;
    return bStart - aStart;
  })
  .slice(0, SITE.NUM_EDUCATION_ON_HOMEPAGE || 3);

// Create Person structured data
const personData = {
  "@context": "https://schema.org",
  "@type": "Person",
  name: "Andrian Lloyd Maagma",
  url: Astro.site,
  email: SITE.EMAIL,
  jobTitle: "Computer Science Student",
  description: SITE.DESCRIPTION,
  sameAs: SOCIALS.map((social) => social.HREF),
};

// Create WebSite structured data with search capability
const websiteData = {
  "@context": "https://schema.org",
  "@type": "WebSite",
  name: "Andrian Lloyd Maagma",
  description: SITE.DESCRIPTION,
  url: Astro.site,
  author: {
    "@type": "Person",
    name: "Andrian Lloyd Maagma",
  },
  potentialAction: {
    "@type": "SearchAction",
    target: {
      "@type": "EntryPoint",
      urlTemplate: `${Astro.site}/#search={search_term_string}`,
    },
    "query-input": "required name=search_term_string",
  },
};
---

<StructuredData data={personData} />
<StructuredData data={websiteData} />

<Layout title={HOME.TITLE} description={HOME.DESCRIPTION}>
  <Container>
    <aside data-pagefind-ignore>
      <div class="space-y-16">
        <section>
          <div class="grid grid-cols-6 items-center gap-4">
            <div class="animate col-span-2 sm:col-span-1">
              <Avatar />
            </div>
            <div
              class="animate col-span-4 space-y-1 sm:col-span-5 sm:space-y-2"
            >
              <h1 class="text-2xl font-semibold sm:text-3xl">
                This is Andrian
              </h1>
              <p class="text-muted-foreground text-xs sm:text-sm">
                <abbr title="Computer Science" class="no-underline">CS</abbr> Student
                @
                <Link href="https://upv.edu.ph" external>
                  <abbr title="University of the Philippines Visayas">UPV</abbr>
                </Link>
              </p>

              <!-- Contact inline on sm and up -->
              <nav aria-label="Contact links" class="mt-2">
                <Contact />
              </nav>
            </div>
          </div>

          <article class="animate space-y-4">
            <p>
              Hi... I'm a Computer Science student specializing in full-stack
              web development, UI/UX design, and data science. I enjoy building
              and maintaining web applications, leading design projects, and
              conducting tech-driven research.
            </p>
          </article>
        </section>

        <section class="animate space-y-6">
          <div class="flex flex-wrap items-center justify-between gap-y-2">
            <h2 class="text-lg font-semibold">Recent projects</h2>
            <Link href="/projects">See all projects</Link>
          </div>

          <div class="animate flex flex-wrap gap-1">
            {
              skills.map((skill) => (
                <SkillBadge skill={skill} class="border-none" />
              ))
            }
            <a
              href="/skills"
              aria-label="See all skills"
              title="See all skills"
            >
              <Badge class="border-none">. . .</Badge>
            </a>
          </div>

          <ul class="not-prose grid grid-cols-1 gap-4 sm:grid-cols-2">
            {
              projects.map((project) => (
                <li>
                  <ProjectCard project={project} />
                </li>
              ))
            }
          </ul>
        </section>

        <section class="animate space-y-6">
          <div class="flex flex-wrap items-center justify-between gap-y-2">
            <h2 class="text-lg font-semibold">Latest posts</h2>
            <Link href="/posts">See all posts</Link>
          </div>

          <div class="animate flex flex-wrap gap-1">
            {tags.map((tag) => <TagBadge tag={tag} class="border-none" />)}
            <a href="/tags" aria-label="See all tags" title="See all tags">
              <Badge class="border-none">. . .</Badge>
            </a>
          </div>

          <ul class="not-prose flex flex-col gap-4">
            {
              posts.map((post) => (
                <li>
                  <PostCard post={post} />
                </li>
              ))
            }
          </ul>
        </section>

        <section class="animate space-y-6">
          <div class="flex flex-wrap items-center justify-between gap-y-2">
            <h2 class="text-lg font-semibold">Recent achievements</h2>
            <Link href="/achievements">See all achievements</Link>
          </div>

          <ul class="not-prose flex flex-col gap-4">
            {
              achievements.map((achievement) => (
                <li>
                  <AchievementCard achievement={achievement} />
                </li>
              ))
            }
          </ul>
        </section>

        {
          stillLearning.length > 0 && (
            <section class="animate space-y-6">
              <div class="flex flex-wrap items-center justify-between gap-y-2">
                <h2 class="text-lg font-semibold">Still learning...</h2>
                <Link href="/education">See education</Link>
              </div>

              <ul class="not-prose flex flex-col gap-4">
                {stillLearning.map((item) => (
                  <li>
                    <EducationCard item={item} />
                  </li>
                ))}
              </ul>
            </section>
          )
        }
      </div>
    </aside>
  </Container>
</Layout>
